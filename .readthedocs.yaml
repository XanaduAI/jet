version: 2

build:
  image: latest
  apt_packages:
    - libopenblas-dev
    - python3.7-dev
    - python3-wheel
    - doxygen
    - graphviz

python:
  version: 3.7
  install:
    - requirements: docs/requirements.txt
    - requirements: python/requirements.txt
    - method: pip
      path: .

sphinx:
  configuration: docs/conf.py
